# Nexus — Example environment variables
# Copy this file to the project root as .env and fill in values.
# Do not commit .env or real secrets to the repo.
# See SETUP.md for full setup instructions.

# -----------------------------------------------------------------------------
# Required
# -----------------------------------------------------------------------------
# Long random string; minimum 32 characters. Used for app security.
NEXUS_SECRET_KEY=your-secret-key-at-least-32-characters-long

# PostgreSQL connection URL. Use asyncpg driver for async support.
# Local Docker default: nexus/nexus on localhost:5432, database nexus.
DATABASE_URL=postgresql+asyncpg://nexus:nexus@localhost:5432/nexus
REDIS_URL=redis://localhost:6379/0

# Celery: broker (queue) and result backend. Often same Redis, different DB index.
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2

# -----------------------------------------------------------------------------
# Optional — API server
# -----------------------------------------------------------------------------
# development | production
NEXUS_ENV=development
# Used for OAuth redirects (e.g. Slack); set to your API base URL in production.
BACKEND_URL=http://localhost:8000

# -----------------------------------------------------------------------------
# Optional — AI analysis (Twitter listening runs)
# -----------------------------------------------------------------------------
# Google Gemini API key for conversation analysis (relevance, suggestions).
# If unset, AI analysis steps may be skipped or use another configured provider.
GOOGLE_API_KEY=

# -----------------------------------------------------------------------------
# Optional — Slack / Google Sheets (notifications and exports)
# -----------------------------------------------------------------------------
SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=
# Path to Google service account JSON (for Sheets export).
GOOGLE_SHEETS_SERVICE_ACCOUNT=
# Default spreadsheet ID for writing opportunities/conversations.
GOOGLE_SHEETS_SPREADSHEET_ID=

# -----------------------------------------------------------------------------
# Optional — Auth (if using frontend with Clerk)
# -----------------------------------------------------------------------------
CLERK_SECRET_KEY=
CLERK_PUBLISHABLE_KEY=
CLERK_BASE_URL=https://api.clerk.dev/v1

# -----------------------------------------------------------------------------
# Optional — Twscrape (Twitter/X search)
# -----------------------------------------------------------------------------
# Proxy URL if needed (e.g. http://user:pass@host:port or socks5://...).
# TWSCRAPE_PROXY=
# Local path for twscrape account DBs (defaults to data/twscrape_accounts if unset).
# TWSCRAPE_STORAGE_PATH=data/twscrape_accounts
